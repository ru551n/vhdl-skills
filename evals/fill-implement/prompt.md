---
max_turns: 4
allowed_tools: [Skill, Read, Glob, Grep]
---

Implement the logic marked with --@ in rtl/fifo_ctrl.vhd: it is a synchronous FIFO controller with an almost-full flag. Make sure it still compiles.
